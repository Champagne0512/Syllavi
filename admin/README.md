# Syllaby 后台管理系统

一个功能完整的网页版后台管理系统，用于管理 Syllaby 微信小程序的各项数据和功能。

## 🚀 功能特性

### 核心功能模块
- **📊 数据总览** - 实时统计图表、系统状态监控
- **👥 用户管理** - 用户列表、详情、数据统计、批量操作
- **📚 课程管理** - 课程审核、统计分析、数据导出
- **🤖 AI管理** - API配置、使用统计、调用日志
- **📈 数据统计** - 用户活跃度、功能使用、留存分析
- **⚙️ 系统设置** - 全局配置、公告管理、操作日志

### 技术特点
- **响应式设计** - 支持桌面端和移动端
- **现代化UI** - Bootstrap 5 + 自定义样式
- **实时数据** - Chart.js 图表可视化
- **安全认证** - JWT令牌验证
- **API集成** - 与Supabase数据库深度集成

## 🛠️ 技术栈

### 后端
- **Node.js** - 运行环境
- **Express** - Web框架
- **PostgreSQL** - 数据库（通过Supabase）
- **JWT** - 身份认证
- **EJS** - 模板引擎

### 前端
- **Bootstrap 5** - UI框架
- **Chart.js** - 数据可视化
- **Bootstrap Icons** - 图标库
- **Vanilla JavaScript** - 交互逻辑

## 📦 安装和运行

### 环境要求
- Node.js >= 16.0
- npm >= 8.0
- PostgreSQL 数据库（通过Supabase）

### 安装步骤

1. **克隆项目**
```bash
cd d:/JAVA/Sallyvi/admin
```

2. **安装依赖**
```bash
npm install
```

3. **配置环境变量**
```bash
cp .env.example .env
# 编辑 .env 文件，填入相关配置信息
```

4. **启动服务**
```bash
# 开发模式
npm run dev

# 生产模式
npm start
```

5. **访问后台**
```
http://localhost:3000
```

### 默认账号
- 用户名: `admin`
- 密码: `admin123`

## 📁 项目结构

```
admin/
├── config/
│   └── database.js          # 数据库配置
├── middleware/
│   └── auth.js             # 认证中间件
├── routes/
│   ├── auth.js             # 认证路由
│   ├── users.js            # 用户管理路由
│   ├── courses.js          # 课程管理路由
│   ├── ai.js               # AI管理路由
│   ├── stats.js            # 数据统计路由
│   └── settings.js         # 系统设置路由
├── views/
│   ├── layout.ejs          # 布局模板
│   ├── login.ejs           # 登录页面
│   ├── dashboard.ejs       # 仪表板
│   ├── users.ejs           # 用户管理
│   ├── courses.ejs         # 课程管理
│   ├── ai.ejs              # AI管理
│   ├── stats.ejs           # 数据统计
│   └── settings.ejs        # 系统设置
├── public/
│   ├── css/
│   │   └── admin.css       # 自定义样式
│   └── js/
│       └── admin.js        # 前端脚本
├── .env.example            # 环境变量示例
├── package.json            # 项目依赖
├── server.js               # 服务器入口
└── README.md               # 项目文档
```

## 🔧 API接口

### 认证相关
- `POST /api/auth/login` - 用户登录
- `GET /api/auth/verify` - 验证令牌
- `POST /api/auth/logout` - 用户登出

### 用户管理
- `GET /api/users` - 获取用户列表
- `GET /api/users/:id` - 获取用户详情
- `GET /api/users/:id/courses` - 获取用户课程
- `GET /api/users/:id/tasks` - 获取用户任务
- `DELETE /api/users/:id` - 删除用户

### 课程管理
- `GET /api/courses` - 获取课程列表
- `GET /api/courses/:id` - 获取课程详情
- `GET /api/courses/stats/overview` - 课程统计
- `GET /api/courses/export/csv` - 导出课程数据
- `DELETE /api/courses/:id` - 删除课程

### AI管理
- `GET /api/ai/stats` - AI使用统计
- `GET /api/ai/config` - AI配置
- `PUT /api/ai/config` - 更新AI配置
- `POST /api/ai/test/:provider` - 测试AI连接
- `GET /api/ai/logs` - AI调用日志

### 数据统计
- `GET /api/stats/overview` - 总览统计
- `GET /api/stats/user-activity` - 用户活跃度
- `GET /api/stats/feature-usage` - 功能使用统计
- `GET /api/stats/school-distribution` - 学校分布
- `GET /api/stats/retention` - 用户留存
- `GET /api/stats/export/:type` - 导出统计数据

### 系统设置
- `GET /api/settings/config` - 系统配置
- `PUT /api/settings/config` - 更新系统配置
- `GET /api/settings/announcements` - 公告列表
- `POST /api/settings/announcements` - 创建公告
- `PUT /api/settings/announcements/:id` - 更新公告
- `DELETE /api/settings/announcements/:id` - 删除公告
- `GET /api/settings/logs` - 操作日志
- `GET /api/settings/health` - 系统健康检查

## 🎨 界面预览

### 登录页面
- 现代化设计风格
- 渐变背景
- 表单验证
- 记住登录状态

### 仪表板
- 实时数据统计卡片
- 用户增长趋势图
- 活跃度分布饼图
- 最新用户列表
- 系统状态监控

### 用户管理
- 搜索和筛选功能
- 用户详情查看
- 批量操作
- 数据导出

### 课程管理
- 课程列表展示
- 课程统计图表
- 课程详情查看
- 数据导出功能

## 🔒 安全特性

- JWT令牌认证
- 请求频率限制
- CORS跨域保护
- SQL注入防护
- XSS攻击防护
- 环境变量加密

## 📊 数据可视化

- Chart.js图表库
- 实时数据更新
- 响应式图表设计
- 多种图表类型支持
- 自定义主题配色

## 🚀 部署指南

### 开发环境
```bash
npm run dev
```

### 生产环境
```bash
# 设置环境变量
export NODE_ENV=production
export PORT=3000

# 启动服务
npm start
```

### Docker部署（可选）
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
EXPOSE 3000
CMD ["npm", "start"]
```

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支
3. 提交代码
4. 推送到分支
5. 创建 Pull Request

## 📝 更新日志

### v1.0.0 (2024-01-20)
- ✨ 初始版本发布
- 🎨 完整的管理界面
- 📊 数据可视化功能
- 🔐 用户认证系统
- 📱 响应式设计

## 📄 许可证

MIT License

## 🙏 致谢

- [Bootstrap](https://getbootstrap.com/) - UI框架
- [Chart.js](https://www.chartjs.org/) - 图表库
- [Express.js](https://expressjs.com/) - Web框架
- [Supabase](https://supabase.com/) - 数据库服务